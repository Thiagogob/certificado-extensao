# O Railway define a porta na variável PORT; fora dele, 80.
:{$PORT:80} {
	root * /srv
	encode gzip
	file_server

	# Qualquer endereço cai no index.html (o site é uma página só).
	try_files {path} /index.html
}
